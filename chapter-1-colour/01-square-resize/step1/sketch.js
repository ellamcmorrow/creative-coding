function setup(){
    createCanvas(720,720); //create a canvas 720 by 720
    colorMode(HSB,360,100,100); // using colour mode HSB, Hue Satuation brighteness, values within the ranges specified
    rectMode(CENTER); //move the imaginary grid so that the rectangle is drawn in the center of the circle
// check inspector to make sure the canvas is there
}
// anything in the draw function will be redrawn each frame
function draw(){


}
